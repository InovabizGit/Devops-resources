parameters:
  - name: EnvironmentName
    type: string
  - name: AzureSubscriptionGuid
    type: string
  - name: AzureFunctionName
    type: string
  - name: ResourceGroupName
    type: string
  - name: ArtifactFolderName
    type: string
jobs:
  - deployment: Deploy
    displayName: Deploy
    environment: ${{parameters.EnvironmentName}}
    pool:
      vmImage: 'ubuntu-latest'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureFunctionApp@1
            displayName: 'Azure Functions App Deploy'
            inputs:
              azureSubscription: '15a17812-5c21-4c8d-9b46-93d6cd001652'
              appType: functionApp
              deployToSlotOrASE: false
              appName: ${{parameters.AzureFunctionName}} 
              resourceGroupName: ${{parameters.ResourceGroupName}}
              package: '$(Pipeline.Workspace)/${{parameters.ArtifactFolderName}}/$(Build.BuildId).zip'